[package]
cairo-version = "=2.9.2"
name = "team_verse"
version = "1.1.0"
edition = "2024_07"

[cairo]
sierra-replace-ids = true

[scripts]
migrate = "sozo build && sozo migrate"                      # scarb run migrate

[dependencies]
dojo = { git = "https://github.com/dojoengine/dojo", tag = "v1.1.1" }
dojo_plugin = { git = "https://github.com/dojoengine/dojo", tag = "v1.1.1" }

[[target.starknet-contract]]
build-external-contracts = ["dojo::world::world_contract::world"]

[dev-dependencies]
cairo_test = "=2.9.2" 
dojo_cairo_test = { git = "https://github.com/dojoengine/dojo", tag = "v1.1.1" }
